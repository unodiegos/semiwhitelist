Config = {
    Engine = '', -- Base Export  | exampel : 'es_extended', 'server_base'

    DiscordRoles = {
        krimRole = '', -- Role ID for Criminal
        pedRole = '', -- Role ID for Ped 
        whitelistRole = '', -- Role ID for Whitelist 
        }
}
